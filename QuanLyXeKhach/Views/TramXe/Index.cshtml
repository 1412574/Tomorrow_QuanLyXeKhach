@{
    Layout = "~/Views/Shared/_LayoutTuyenXe.cshtml";
}

@model IEnumerable<DataModel.TramXe>


<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">QUẢN LÝ TRẠM XE</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-success">
                <div class="panel-heading">
                    Danh sách trạm xe
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th>STT</th>
                                <th>
                                    Tên trạm xe
                                </th>
                                <th>
                                    Địa chỉ
                                </th>
                                <th>Lựa chọn</th>
                            </tr>
                        </thead>
                        <tbody>
                            @{int i = 1;}
                            @foreach (var item in Model)
                            {

                                <tr class="odd gradeX">
                                    <td>@i</td>
                                    @{i++; }
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TenTramXe)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.DiaChi)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Edit", "Edit", new { id = item.MaTramXe }) |
                                        @Html.ActionLink("Details", "Details", new { id = item.MaTramXe }) |
                                        @Html.ActionLink("Delete", "Delete", new { id = item.MaTramXe })
                                    </td>
                                </tr>
                                        }

                        </tbody>
                    </table>
                    <!-- /.table-responsive -->

                </div>
                <!-- /.panel-body -->
                <div style="padding-left: 10px; padding-bottom: 10px">
                    <input type="button" class="btn btn-lg btn-info" value="Thêm trạm xe" onclick="location.href='@Url.Action("Create","TramXe")'" />
                </div>
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-12 -->
    </div>

    <!-- /.row -->
</div>